import 'package:flutter/material.dart';

class OgrenciScreen extends StatelessWidget {
  const OgrenciScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('OgrenciScreen')),
    );
  }
}
