import 'package:flutter/material.dart';

class YoneticiScreen extends StatelessWidget {
  const YoneticiScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Yonetici')),
    );
  }
}
